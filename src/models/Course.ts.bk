import isUuid from "../validators/uuid"

export default class Course {
  constructor (
    id: string,
    displayId: string,
    category: number,
    name: string,
  ) {
  }
  setId (id: string) {
    this.#id = id
  }
}
